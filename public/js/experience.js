
/*
function moveCamera() {
    const t = document.body.getBoundingClientRect().top;
    sphereMesh.rotation.x = t * 0.01;
}
document.body.onscroll = moveCamera
/*
const tx = Math.cos( 2 * Math.PI * t ) ;
const ty =  t * 5 + this.offsety;
const tz = Math.sin( 2 * Math.PI * t );
*/

